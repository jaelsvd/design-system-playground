import { useId } from 'react'; import styles from './Switch.module.scss'
export interface SwitchProps { checked: boolean; onCheckedChange: (checked: boolean) => void; label: string; description?: string; disabled?: boolean }
export function Switch({ checked, onCheckedChange, label, description, disabled }: SwitchProps) { const id = useId(); return <div className={styles.wrap}><button id={id} type="button" role="switch" aria-checked={checked} aria-describedby={description ? `${id}-description` : undefined} className={styles.switch} data-state={checked ? 'checked' : 'unchecked'} disabled={disabled} onClick={() => onCheckedChange(!checked)}><span className={styles.thumb}/></button><div className={styles.copy}><label htmlFor={id} className={styles.label}>{label}</label>{description && <span id={`${id}-description`} className={styles.description}>{description}</span>}</div></div> }
